# OPF 隐私信息检测平台配置
# 复制此文件为 .env 并按需修改

# Web 服务端口
WEB_PORT=8081

# OPF 检测并发数（默认 5，内存紧张可调低）
OPF_CONCURRENCY=5

# 任务自动清理（超过此小时数的任务自动删除，默认 72 小时）
TASK_MAX_AGE_HOURS=72

# 内存中最大任务数（超过此数量自动清理最旧任务，默认 500）
TASK_MAX_COUNT=500

# CORS 允许的域名（逗号分隔，默认 * 允许所有，生产环境改为具体域名）
CORS_ORIGINS=*
